{
  "build": { "dockerfile": "Dockerfile" },

  // Use 'postCreateCommand' to run commands after the container is created. 
  // The action below is take place in the /workspace/github-repo-name folder. 
  // The Dockerfile cannot be used for this part of the container setup since 
  // it runs before the dev container is created and the workspace folder is 
  // mounted and therefore does not have access to the files in the workspace folder. 
  // "postCreateCommand": "julia -e 'using Pkg; Pkg.activate(\"/workspaces/devcontainer-test\"); Pkg.instantiate()'",
  "postCreateCommand": "julia -e 'using Pkg; Pkg.activate(\"/workspaces/devcontainer-test\"); Pkg.instantiate()'",
}
